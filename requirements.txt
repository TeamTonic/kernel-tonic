# =========================
# ROCm/MI300X Requirements
# =========================

# PyTorch, TorchVision, and Torchaudio for ROCm/MI300X
# DO NOT install from PyPI. Instead, follow the official ROCm install instructions:
# https://rocm.docs.amd.com/projects/install-on-linux/en/develop/install/3rd-party/pytorch-install.html
# Example (for ROCm 6.x, Python 3.10):
# pip install --pre torch torchvision torchaudio --index-url https://download.pytorch.org/whl/rocm6.0

# AMD ROCm Python bindings
hip-python>=0.1.0

# Transformer Engine (ROCm support is experimental; use with caution)
transformer-engine>=1.14.0

# vLLM for inference (ROCm support is experimental; use with caution)
vllm

# Quantization support
# auto-gptq  # CPU quantization only; GPU quantization is CUDA-only

# Hugging Face ecosystem
transformers>=4.35.0
tokenizers>=0.15.0
datasets>=2.14.0
accelerate>=0.24.0
peft>=0.7.0

# Training and optimization
# deepspeed  # ROCm support is experimental; uncomment if needed and test carefully
wandb>=0.16.0
tensorboard>=2.14.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0

# Configuration and utilities
pyyaml>=6.0
hydra-core>=1.3.0
omegaconf>=2.3.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Development tools
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0
mypy>=1.5.0

# Additional utilities
tqdm>=4.65.0
rich>=13.0.0
click>=8.1.0 